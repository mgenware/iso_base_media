name: iso_base_media
description: A dart package to parse ISO Base Media File Format and MP4 files.
version: 3.2.2
repository: https://github.com/mgenware/iso_base_media

environment:
  sdk: ^3.4.3

# Add regular dependencies here.
dependencies:
  random_access_source: ^1.0.1
  # path: ^1.8.0

dev_dependencies:
  mgenware_dart_lints: ^6.5.2
  test: ^1.24.0
